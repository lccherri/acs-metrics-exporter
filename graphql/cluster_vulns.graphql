query getClusterVulnerabilities {
  clusterVulnerabilities {
    cve
    severity
    isFixable
    cvss
    scoreVersion
    summary
    link
    publishedOn
    vulnerabilityType
    clusterCount
    clusters {
        id
        name
        type
    }
  }
}
